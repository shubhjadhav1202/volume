#include<iostream>
using namespace std;
int volume(int l)
{
return (l*l*l);
}

long volume(int l,int b,int h)
{
 return (l*b*h);
}
int main()
{
cout<<"Volume of Cube :"<<volume(9)<<endl;
cout<<"Volume of Box :"<<volume(9,4,6)<<endl;
}
